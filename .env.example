# ローカルは .env.local にコピーして値を設定（.env.local は Git に含めない）
# 本番（Vercel 等）ではダッシュボードの Environment Variables に設定

GEMINI_API_KEY=
# 任意（既定: gemini-2.0-flash）
# GEMINI_MODEL=gemini-1.5-flash

# Dify を使う場合のみ
# DIFY_API_KEY=
